; Copyright (c) 2026 Tom Chapman. All rights reserved.
; PaintForge - input configuration guard file (pkg-core).
;
; Intentionally empty: UE 5.6 already defaults DefaultPlayerInputClass=EnhancedPlayerInput and
; DefaultInputComponentClass=EnhancedInputComponent (02 §4.3). This file exists so a stale
; template can never silently override those defaults; add explicit lines here only if that
; ever happens. All input actions/contexts are constructed natively in UPFInputConfig.
